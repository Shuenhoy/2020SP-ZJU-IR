#pragma once

#include <common/common.hpp>
#include <common/doc_inv_index.hpp>
#include <vector>

namespace ir::ir {
inline std::vector<common::DocInvIndexElement> phrase_merge(const std::vector<common::DocInvIndexElement> &a,
                                                            const std::vector<common::DocInvIndexElement> &b) {
    NOT_IMPLEMENTED;
}
} // namespace ir::ir
